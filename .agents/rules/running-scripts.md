---
trigger: always_on
---

If you need to write a short utility script to do some automated code analysis, you must write the script as a TypeScript script in a single `.ts` file, and then run the script with `node <filename>`.